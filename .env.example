# Django settings
SECRET_KEY=django-insecure-change-me
DEBUG=0
ALLOWED_HOSTS=localhost,127.0.0.1

# Database configuration
DB_NAME=doc_bd
DB_USER=postgres
DB_PASSWORD=change-this-password
DB_HOST=localhost
# When running with docker-compose use DB_HOST=db
DB_PORT=5432

# Email configuration (optional)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=
